{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Formulaire enregistrement</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="Valider" class="btn btn-primary btn-block" formnovalidate>
    {{ form_end(form) }}


    <a href="{{path("app_personne_show")}}">
    Liste des personnes
    </a>


{% endblock %}
